[package]
name = "amll-player-core"
version = "0.1.0"
edition = "2024"

[features]
default = []

[dependencies]
anyhow = "^1.0"
cpal = "^0.17"
crossbeam-channel = "0.5.15"
crossbeam-utils = "0.8.21"
futures = "0.3"
md5 = "^0.8"
rb = "^0.4"
ringbuf = "^0.5"
arrayvec = "^0.7"
bs1770 = "1.0"
segmap = "0.1"
serde = { version = "^1.0", features = ["derive"] }
serde_json = "^1.0"
spectrum-analyzer = "1.5"
tokio = { version = "^1", features = [
    "time",
    "macros",
    "sync",
    "rt",
    "rt-multi-thread",
] }
tracing = "0.1"
parking_lot = "0.12"
ffmpeg_audio = { version = "0.2", features = ["tracing"] }
now-playing-controls = { git = "https://github.com/apoint123/now-playing-controls.git" }
tokio-util = "0.7.18"
reqwest = { version = "0.13", default-features = false, features = [
    "rustls",
    "stream",
] }

[dev-dependencies]
tracing-subscriber = "0.3"
windows = { version = "0.62", features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_Graphics_Gdi",
] }

[build-dependencies]
